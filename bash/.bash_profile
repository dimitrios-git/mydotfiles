# bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi

# User specific environment and startup programs

# Add directories to PATH
export PATH="/home/dscharalampidis/.local/bin:$PATH"
